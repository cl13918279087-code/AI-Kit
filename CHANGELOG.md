# Changelog

## [1.3.0] - 2026-09-12

响应成员 Issue #3（Word 五处漏脱敏）/#4（项目分行误伤）的整改发布，含行为口径变化。

### Fixed
- **跨 run 漏脱敏（R-A，结构性）**：regex 兜底从 per-run 升级为**段落级**——Word 常把一句话拆进多个 run（拼写检查/字体切换），"2015年4月|3日"、跨 run 邮箱/分行名此前全部漏检。整段跑规则后用 SequenceMatcher 差异区间精确回写各 run，未变更 run 格式保留（`_redistribute_paragraph`）
- **分行/支行名漏检（R-B）**：地址通道回溯时"乡/路/街/巷/弄/道/村"此前被误判为硬边界，"新乡分行""金水路支行"整段处理也漏检；现对支行/分行后缀作为地名成分收集
- **"项目分行"误伤（R-B，Issue #4）**：地址通道把"项目/产品/工程"收为地名前缀导致"大集中项目分行→大集中XX分行"；新增前缀词守卫
- **"中牟县支行"漏检（存量）**：回溯停在停用词"中"（为"其中"保留）致前缀仅1字被保护跳过；跨行政后缀场景下"中"为县名首字时并入
- **docProps 泄露（R-C）**：补齐 app.xml（`<Company>` 字段泄露银行名）/ custom.xml（自定义属性）处理；footnotes/endnotes 经核查已由 w:t 遍历覆盖
- **LLM 兜底阻断问题**：regex 兜底此前跳过 LLM 已修改节点，导致同段落剩余部分失去跨 run 上下文；现段落级统一处理（占位符幂等已验证）

### Changed
- **固话口径（R-D，社区决策）**：由"0XX-保留后8位"改为**全遮**（0XX-XXXXXXXX），消除号码反查风险；历史等长替换约束随段落级回写取消

### Verified
- Issue #3 全部场景复测通过（漏1/3/5 跨run、漏1 分行名、漏4 属性区）
- 误伤回归 10 例、漏检回归 6 例、一致性 22 例通过
- UAT3 比对基线持平（2/6）、差异分析持平（5/7）；真实文档敏感词零残留

## [1.2.3] - 2026-09-12

响应成员 Issue #2 的整改发布（社区运行模式首个完整闭环）。

### Fixed
- **Excel 计数恒为 0**（Issue #2，成员报告）：`redact_excel.py` 的 `_process_xml_file` 此前无条件 `return {}`，共享字符串/工作表/批注/页眉页脚/文档属性 5 条路径计数全部丢失；现返回分类计数并全路径接入
- **openpyxl 产物中文漏脱敏**（巡检新发现）：openpyxl 等工具生成的 xlsx 把中文写成 `&#NNNNN;` 数字实体，规则层在 XML 源码上无法识别汉字——中文姓名/中文日期全部漏脱敏；现脱敏前解码 CJK 区段实体（Excel/WPS 原生文件不受影响，属健壮性修复）

### Changed
- **PPT 计数升级**：从"按文件数"（`{"XML文件": 1}`）升级为"按脱敏处数"分类计数，布局/母版路径接入，与 word/pdf 口径一致
- `common_rules.py` 新增 `apply_redactions_counted(text) -> (str, dict)`：三阶段（前置规则/地址通道/姓名规则）逐处计数，按占位符类别归类（姓名/日期/银行名/邮箱/固话/地址等）；`apply_redactions` 重构为其兼容包装，行为不变（15 例一致性回归通过）

### Verified
- xlsx 端到端：17 处计数与实际替换对齐，敏感词零残留（含 openpyxl 实体场景 8 处姓名）
- word 通道回归：UAT3 文档 7 处，与 v1.2.2 一致

## [1.2.2] - 2026-09-06

基于《redact_docx_v2改进项清单》评估意见的整改发布（commit bddc90b）。

### Fixed（R1-R3）
- **R1 姓名误伤治理**：姓名规则接入常用词覆盖保护（`_name_guard_sub`）——此前 `EXCLUDED_COMMON_WORDS` 定义了但从未使用；扩充 50+ 实测误伤宿主词。"说明书→说XXX" 类误伤消除（实测误伤率 90% → 0）
- **R2 日期漏脱敏（重要）**：修复斜杠/横杠日期尾部 `$` 锚点 bug——正文中日期此前**永不命中**；修复"2020年5月→YYYY/MM/DD月"残字；新增无年份短日期 X月X日、M/D 规则
- **R3 银行名兜底**：新增通用模式 `[汉字]{2,6}银行`，白名单外银行名（如宁夏银行）不再被姓名规则截胡为"宁XXX行"

### Added（R6-R7）
- **R6**：页眉/页脚 `.rels` 引用图片确定性全图遮盖，不再依赖尺寸启发式
- **R7**：静默失败治理——遮盖失败、EMF/WMF 矢量图显式列入人工检查清单并计数汇报

### Verified
- 误伤 8 例消除、姓名脱敏回归 5 例、日期 7 例、银行名 3 例通过
- 端到端 UAT3 docx 敏感词零残留；比对基线持平（2/6）

## [1.2.1] - 2026-09-05

- fix Word XML node offset + enable fixed phone redact + contact name rule（64a3cf8）
- 完善姓名脱敏规则，修复多字姓名/地址通道误伤/XML上下文边界等问题（bdc4661）

## [1.2.0] - 2026-09-05

- 比对工具上下文指纹对齐重构，消除对齐噪声（939f17a）
- extract_text_from_xml 重复提取修复；OLE2 .doc LibreOffice 转换通道
- 地址回溯式脱敏通道（24 类后缀）、输出文件名脱敏、技能包规范化结构
