# Changelog

## [1.3.4] - 2026-09-13

### 流程（响应贡献者排名需求）
- 社区规范新增**提交者署名**机制：
  - Issue 模板（缺陷报告 / 改进建议）增加"提交者署名"必填项
  - 《社区运行模式》第五章第 3 步补充署名规则（格式/回溯标注/未署名处理）
  - 新增第十一章"贡献档案（提交者署名）"，明确署名入口、档案维护、排名口径
  - 附录 A 清单模板头部增加 `编制人` 字段
  - 新增 `docs/贡献档案.md`：Issues #2–#10 贡献记录（署名待成员认领回填）
- 社区规范补充**无 GitHub 账户成员提交流程**（离线代开）：
  - 成员离线提交清单 → 维护者代开 Issue 并署名 → 同 Issue 闭环 → 贡献档案照常计分
  - 权限红线：token 仅维护者持有；禁止创建公共共享账户或分发 token（违反 GitHub ToS、无法追责、与署名机制冲突）

## [1.3.3] - 2026-09-13

响应成员 Issue #10（v1.3.2 复测反馈）的整改发布。

### Fixed
- **PPTX 跨 run 漏脱敏（R-⑥，Issue #10-A，P0，结构性）**：新增 `<a:p>` 段落级通道——PPT 与 Word 一样把一句话拆进多个 `<a:t>` run（封面 `'2017'+'年'+'3'+'月'`、"…、黎"+"萍、方"+"培培、"），per-run 处理全部漏检且产生"干黎→XXX"连带误伤。现段落级拼接脱敏后用 SequenceMatcher 差异区间回写各 run（工具 `redistribute_paragraph` 上移至 common_rules 供 DOCX/PPTX 共用）；幻灯片/布局/母版/备注四类 XML 全部接入，整文件级通道保留兜底（占位符幂等）
- **罕见姓氏/名字池缺口（R-⑦，Issue #10-A）**：surname_pool 补 申/黎/范（申学清/黎萍/范娜），name_pool 补 宝/香/莎（刘宝香/姚莎）；"黎明"入排除词（"申请"经核验不受影响——"请"不在名字池）
- **英文人名漏脱敏（R-⑧，Issue #10-A）**：新增角色上下文英文人名规则——段落含 组长/成员/经理/负责人/联系人 等角色词时，段内 `[A-Z][a-z]{2,}` 独立词遮蔽（"业务组长：…（Lisa）"命中）；不做无差别英文遮蔽，UAT/POS 等术语不受影响
- **XLSX 二字姓名误伤（R-⑨，Issue #10-B，P2，结构性）**：新增前向双字组合守卫——二字姓名命中首字与前一字构成常用组合（交易/资金/消费/历史/收支/存单/记账 等）时保留原文，一次性覆盖 易明(交易明细)/金周(资金周转)/费成(消费成功)/单正(存单)/史明(历史明细) 等 12 类误伤；仅作用于 2 字命中，避免"王进+行"类误保护。**否决成员"姓名匹配两侧须为非汉字"建议**（"联系人张三"类真实姓名前为汉字，会造成 P0 漏脱）
- **排除词残余补充（R-⑩）**：时不用、部支一七、部提七、金进行（前向守卫覆盖不到的组合）

### Verified
- 启动会 pptx：Lisa/申学清/黎萍/方培培/刘宝香/姚莎/范娜/2017年3月 全部遮蔽，"骨干"等常用词零误伤
- 尖刀 xlsx：成员 6 例误伤场景全部归零，真实姓名遮蔽无回退
- 五份基线全量复跑通过；英文规则/二字守卫/历史修复点回归通过；PPTX 重新打包后 python-pptx 可正常打开
- Issue #10-C 判定为标准版人工口径差异（空格断词批量替换），G2 以真实敏感信息零残留为准，不改码

## [1.3.2] - 2026-09-12

响应成员 Issue #6/#7/#8/#9 的整改发布（4 条建议经实测全部属实）。

### Fixed
- **日期范围 `\2` 残字符（Issue #8，P0）**：v1.2.3 计数重构把字符串替换以 callable 形式传给 `pattern.sub`，callable 返回值不再展开反向引用——三类日期范围规则（中文"年月日至"/斜杠/横杠）的替换串中 `\2`（指代"至"分隔符）以字面量落盘（如 `YYYY年MM月DD日\2YYYY年MM月DD日`），暴露范围结构。现字符串替换统一经 `m.expand()` 展开后再落盘（纯文本替换串恒等，零副作用）
- **罕见姓氏漏脱敏（Issue #7，P0）**：surname_pool 缺 栗/亓/邱，name_pool 缺 瑛/南/一，致"栗志芳/亓琦/邱向坤/李南/赵一飞"等 6 类姓名完全漏脱、"X瑛"部分残留。现补充姓氏 11 字（栗亓邱邝覃仝逄郜逯芮綦）、名字用字 3 字（瑛南一）；`一` 字场景依赖既有常用词保护（第一/万一/单一等已入表）
- **姓名误伤（Issue #6，P2）**：章/练/时/易 等姓氏字与常用词撞字（章程→"管理XXX维护"、演练方案→"演XXX案"、和业务→"测试组XXX务团队"）。基线语料全量提取姓名命中片段逐一核查后，EXCLUDED_COMMON_WORDS 补充 章程/演练方案/和业务/交易一部/时业务。经核验此为规则层通病而非 Excel 特有
- **.doc 假脱敏泄漏路径（Issue #9）**：环境无 LibreOffice 时 `.doc` 此前仅告警并复制原文件充当输出——输出名带"脱敏"但内容未脱敏，属潜在 P0 泄漏。现改为**报错终止、不生成任何输出文件**，并强制列入人工检查清单（含安装指引与"按未脱敏文件管理"提示）

### Docs
- README / 项目实施手册：补 .doc 处理的 LibreOffice 依赖说明（含 Windows 安装指引）与 v1.3.2 安全策略变更说明

### Verified
- Issue #8 复现用例：三类日期范围全部正确展开（`YYYY年MM月DD日至YYYY年MM月DD日` 等）
- Issue #7 六个漏脱姓名全部整体遮蔽；Issue #6 三例误伤归零且 10 例真实姓名遮蔽不回退
- 五份基线文档复跑门禁：全输出零 `\2`/`\1` 残留（详见发布说明）

## [1.3.1] - 2026-09-12

响应成员 Issue #5（姓名紧邻数字时部分残留）的整改发布，含连带发现的两类存量漏脱敏。

### Fixed
- **姓名部分匹配（Issue #5）**：全部 6 条姓名规则（A/复姓A/B/C-1/C/D/E）的贪婪量词与右边界 `(?![a-zA-Z0-9])` 组合，在姓名后紧跟数字/字母时回溯降级为部分匹配——"方培培0371"→"XXX培0371"（残留"培"）。**经基线复核此为存量缺陷而非 v1.3.0 回归**（bdc4661 同样复现，成员的回归论断有误）；现移除右边界，姓名整体匹配
- **`\b` 词边界漏脱敏（连带发现，同类排查）**：Python 中汉字与数字均为 `\w`，"汉字+数字"直连处 `\b` 不成立——"手机13812345678"、"身份证110101..." **此前完全漏脱敏**；身份证/手机号/IP/MAC 规则的 `\b` 全部替换为数字感知边界（`(?<!\d)` 类），CJK 直连场景现已命中
- **无分隔紧凑固话（Issue #5 矩阵）**：新增 `0\d{9,11}` 规则（如"方培培037185519208"），前后数字边界防止误吞身份证/账号片段

### Verified
- Issue #5 完整矩阵 9 例通过；姓名回归 9 例、误伤回归 8 例、新旧一致性 16 例通过
- 比对基线：PMO 0/3、差异分析 5/7、UAT3 2/6、尖刀内容 31/276 持平；郑州 pptx 误检 +1、福建 误检 +17——经逐例核查为**标准版自身未脱敏的"姓名+数字"场景**（如"张丹39A56"旧代码残留"丹"字，现整体脱敏），属合规性提升

## [1.3.0] - 2026-09-12

响应成员 Issue #3（Word 五处漏脱敏）/#4（项目分行误伤）的整改发布，含行为口径变化。

### Fixed
- **跨 run 漏脱敏（R-A，结构性）**：regex 兜底从 per-run 升级为**段落级**——Word 常把一句话拆进多个 run（拼写检查/字体切换），"2015年4月|3日"、跨 run 邮箱/分行名此前全部漏检。整段跑规则后用 SequenceMatcher 差异区间精确回写各 run，未变更 run 格式保留（`_redistribute_paragraph`）
- **分行/支行名漏检（R-B）**：地址通道回溯时"乡/路/街/巷/弄/道/村"此前被误判为硬边界，"新乡分行""金水路支行"整段处理也漏检；现对支行/分行后缀作为地名成分收集
- **"项目分行"误伤（R-B，Issue #4）**：地址通道把"项目/产品/工程"收为地名前缀导致"大集中项目分行→大集中XX分行"；新增前缀词守卫
- **"中牟县支行"漏检（存量）**：回溯停在停用词"中"（为"其中"保留）致前缀仅1字被保护跳过；跨行政后缀场景下"中"为县名首字时并入
- **docProps 泄露（R-C）**：补齐 app.xml（`<Company>` 字段泄露银行名）/ custom.xml（自定义属性）处理；footnotes/endnotes 经核查已由 w:t 遍历覆盖
- **LLM 兜底阻断问题**：regex 兜底此前跳过 LLM 已修改节点，导致同段落剩余部分失去跨 run 上下文；现段落级统一处理（占位符幂等已验证）

### Changed
- **固话口径（R-D，社区决策）**：由"0XX-保留后8位"改为**全遮**（0XX-XXXXXXXX），消除号码反查风险；历史等长替换约束随段落级回写取消

### Verified
- Issue #3 全部场景复测通过（漏1/3/5 跨run、漏1 分行名、漏4 属性区）
- 误伤回归 10 例、漏检回归 6 例、一致性 22 例通过
- UAT3 比对基线持平（2/6）、差异分析持平（5/7）；真实文档敏感词零残留

## [1.2.3] - 2026-09-12

响应成员 Issue #2 的整改发布（社区运行模式首个完整闭环）。

### Fixed
- **Excel 计数恒为 0**（Issue #2，成员报告）：`redact_excel.py` 的 `_process_xml_file` 此前无条件 `return {}`，共享字符串/工作表/批注/页眉页脚/文档属性 5 条路径计数全部丢失；现返回分类计数并全路径接入
- **openpyxl 产物中文漏脱敏**（巡检新发现）：openpyxl 等工具生成的 xlsx 把中文写成 `&#NNNNN;` 数字实体，规则层在 XML 源码上无法识别汉字——中文姓名/中文日期全部漏脱敏；现脱敏前解码 CJK 区段实体（Excel/WPS 原生文件不受影响，属健壮性修复）

### Changed
- **PPT 计数升级**：从"按文件数"（`{"XML文件": 1}`）升级为"按脱敏处数"分类计数，布局/母版路径接入，与 word/pdf 口径一致
- `common_rules.py` 新增 `apply_redactions_counted(text) -> (str, dict)`：三阶段（前置规则/地址通道/姓名规则）逐处计数，按占位符类别归类（姓名/日期/银行名/邮箱/固话/地址等）；`apply_redactions` 重构为其兼容包装，行为不变（15 例一致性回归通过）

### Verified
- xlsx 端到端：17 处计数与实际替换对齐，敏感词零残留（含 openpyxl 实体场景 8 处姓名）
- word 通道回归：UAT3 文档 7 处，与 v1.2.2 一致

## [1.2.2] - 2026-09-06

基于《redact_docx_v2改进项清单》评估意见的整改发布（commit bddc90b）。

### Fixed（R1-R3）
- **R1 姓名误伤治理**：姓名规则接入常用词覆盖保护（`_name_guard_sub`）——此前 `EXCLUDED_COMMON_WORDS` 定义了但从未使用；扩充 50+ 实测误伤宿主词。"说明书→说XXX" 类误伤消除（实测误伤率 90% → 0）
- **R2 日期漏脱敏（重要）**：修复斜杠/横杠日期尾部 `$` 锚点 bug——正文中日期此前**永不命中**；修复"2020年5月→YYYY/MM/DD月"残字；新增无年份短日期 X月X日、M/D 规则
- **R3 银行名兜底**：新增通用模式 `[汉字]{2,6}银行`，白名单外银行名（如宁夏银行）不再被姓名规则截胡为"宁XXX行"

### Added（R6-R7）
- **R6**：页眉/页脚 `.rels` 引用图片确定性全图遮盖，不再依赖尺寸启发式
- **R7**：静默失败治理——遮盖失败、EMF/WMF 矢量图显式列入人工检查清单并计数汇报

### Verified
- 误伤 8 例消除、姓名脱敏回归 5 例、日期 7 例、银行名 3 例通过
- 端到端 UAT3 docx 敏感词零残留；比对基线持平（2/6）

## [1.2.1] - 2026-09-05

- fix Word XML node offset + enable fixed phone redact + contact name rule（64a3cf8）
- 完善姓名脱敏规则，修复多字姓名/地址通道误伤/XML上下文边界等问题（bdc4661）

## [1.2.0] - 2026-09-05

- 比对工具上下文指纹对齐重构，消除对齐噪声（939f17a）
- extract_text_from_xml 重复提取修复；OLE2 .doc LibreOffice 转换通道
- 地址回溯式脱敏通道（24 类后缀）、输出文件名脱敏、技能包规范化结构
